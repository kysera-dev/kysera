# Force building better-sqlite3 from source in CI
# This ensures compatibility with the target platform
better-sqlite3_build_from_source=true

# Use python3 for building native modules
python=python3

# Enable pnpm strict mode for better reliability
strict-peer-dependencies=false
auto-install-peers=true